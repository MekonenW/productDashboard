<h1>All Comments</h1>

<table border='1'>
     <tr>
      <td>Comments</td>
      <td>Products</td>
    </tr>
  <% @comments.each do |comment| %>
    <tr>
      <td><%= comment.comment %></td>
      <td><%= comment.product.name %></td>
    </tr>
  <% end %>

</table>

<a href="/">Back</a>
